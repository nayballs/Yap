{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Blip default capability for the pill window",
  "windows": ["pill"],
  "permissions": [
    "core:default"
  ]
}
